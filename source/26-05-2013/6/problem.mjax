Задана битовая матрица <script type="math/tex">n\times n</script> с элементами <script type="math/tex">0</script> и <script type="math/tex">1</script> (каждый элемент матрицы занимает один бит памяти). 
Назовем строку (столбец) исходной матрицы плохой (плохим), если в нем встречается хотя бы один ноль. Необходимо в исходной матрице занулить все плохие строки и столбцы. 
Предложите алгоритм, решающий эту задачу за <script type="math/tex">O(1)</script> дополнительной памяти и оцените его временную стоимость.
